{
  "name": "matrix-click-average",
  "version": "1.0.0",
  "description": "An average calculation of many clicks on a graph matrix",
  "scripts": {
    "build:css": "cleancss -o style.min.css style.css",
    "build:js": "babel ./index.js | uglifyjs --compress --mangle > index.min.js",
    "build": "npm run build:js && npm run build:css",
    "start": "http-server -c-1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daniel Perez",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/preset-env": "^7.2.3",
    "clean-css-cli": "^4.2.1",
    "http-server": "^0.11.1",
    "uglify-js": "^3.4.9"
  }
}
